import 'package:flutter/material.dart';

class StartSettingScreen extends StatelessWidget {
  const StartSettingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
